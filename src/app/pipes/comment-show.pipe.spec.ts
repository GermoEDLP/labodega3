import { CommentShowPipe } from './comment-show.pipe';

describe('CommentShowPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentShowPipe();
    expect(pipe).toBeTruthy();
  });
});
